# bin

Executable gates for this tree. `check-public-tree.sh` and `export-to-public.sh` are byte-identical to kumo-trading-platform's (one
script, two trees; the export copies it) and refuses what a public tree must never hold; `hooks/`
holds the Claude Code commit gate that runs it. Goes here: gates and their hooks. Does not go here:
research scripts, data, credentials.
