name: public-tree

on:
  pull_request:
  push:
    branches: [main]

jobs:
  check-public-tree:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - run: bin/check-public-tree.sh
