# best

Momentum rotation as MOMENTUM ships it — one open-anchored decision per session, lookback 20, eight names, give-back
exits — over the ledger-book-daily-2024-2026 set. `python run.py` writes `results/`.
