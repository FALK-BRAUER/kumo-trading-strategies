# variations

One folder per configuration run against this set: `run.py`, `parameters.json`, `results/`. `best` is the momentum rotation as MOMENTUM ships it; `bctrot` the (not yet successful) BCTROT-schedule variation.
