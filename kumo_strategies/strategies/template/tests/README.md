# template/tests

Tests for the template strategy — its Nautilus lane (`../nautilus.py`) and, where one exists, its session
runner (`../runner.py`) and pure layer.

Holds: tests whose subject is THIS strategy. Does NOT hold: cross-lane conformance sweeps
(→ `runtime/tests/`), or research scripts (→ `../backtests/`).
